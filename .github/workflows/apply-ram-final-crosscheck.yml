name: Apply RAM Final Cross-Check

on:
  push:
    branches:
      - experiment/full-ram-epg-engine
    paths:
      - ci/apply-ram-final-crosscheck.txt

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: experiment/full-ram-epg-engine
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.13'
      - name: Apply final TiViMate and PR24 cross-check fixes
        run: python ci/patch-ram-final-crosscheck.py
      - name: Commit final cross-check fixes
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No product changes needed"
            exit 0
          fi
          git config user.name "Charm RAM Crosscheck Bot"
          git config user.email "actions@users.noreply.github.com"
          git add frontend
          git commit -m "fix(ram): finish TiViMate and PR24 cross-check"
          git push origin HEAD:experiment/full-ram-epg-engine
