diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/HlsAudioRecoveryPlaylistParserFactory.java b/node_modules/expo-video/android/src/main/java/expo/modules/video/HlsAudioRecoveryPlaylistParserFactory.java
new file mode 100644
index 0000000..696ba74
--- /dev/null
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/HlsAudioRecoveryPlaylistParserFactory.java
@@ -0,0 +1,117 @@
+package expo.modules.video;
+
+import android.net.Uri;
+import androidx.annotation.Nullable;
+import androidx.media3.common.util.UnstableApi;
+import androidx.media3.exoplayer.hls.playlist.DefaultHlsPlaylistParserFactory;
+import androidx.media3.exoplayer.hls.playlist.HlsMediaPlaylist;
+import androidx.media3.exoplayer.hls.playlist.HlsMultivariantPlaylist;
+import androidx.media3.exoplayer.hls.playlist.HlsPlaylist;
+import androidx.media3.exoplayer.hls.playlist.HlsPlaylistParserFactory;
+import androidx.media3.exoplayer.upstream.ParsingLoadable;
+import java.io.IOException;
+import java.io.InputStream;
+import java.util.ArrayList;
+import java.util.LinkedHashSet;
+import java.util.List;
+import java.util.Locale;
+import java.util.Set;
+
+/**
+ * Conservatively repairs common IPTV HLS defects around separate AUDIO groups:
+ * <ul>
+ *   <li>variants reference a different AUDIO group spelling than the one
+ *       declared by {@code EXT-X-MEDIA} (case/whitespace insensitive)</li>
+ *   <li>variants omit {@code AUDIO=} while the master declares exactly one
+ *       external audio rendition and no muxed audio format</li>
+ * </ul>
+ * There is no safe general repair when the provider omits the audio URI
+ * entirely, so this class never invents an unlinked playlist URL.
+ */
+@UnstableApi
+public final class HlsAudioRecoveryPlaylistParserFactory implements HlsPlaylistParserFactory {
+  private final DefaultHlsPlaylistParserFactory delegate = new DefaultHlsPlaylistParserFactory();
+
+  @Override
+  public ParsingLoadable.Parser<HlsPlaylist> createPlaylistParser() {
+    return new RepairingParser(delegate.createPlaylistParser());
+  }
+
+  @Override
+  public ParsingLoadable.Parser<HlsPlaylist> createPlaylistParser(
+      HlsMultivariantPlaylist multivariantPlaylist,
+      @Nullable HlsMediaPlaylist previousMediaPlaylist) {
+    return new RepairingParser(delegate.createPlaylistParser(multivariantPlaylist, previousMediaPlaylist));
+  }
+
+  private static final class RepairingParser implements ParsingLoadable.Parser<HlsPlaylist> {
+    private final ParsingLoadable.Parser<HlsPlaylist> delegate;
+
+    RepairingParser(ParsingLoadable.Parser<HlsPlaylist> delegate) {
+      this.delegate = delegate;
+    }
+
+    @Override
+    public HlsPlaylist parse(Uri uri, InputStream inputStream) throws IOException {
+      return repair(delegate.parse(uri, inputStream));
+    }
+  }
+
+  private static boolean groupIdsMatch(@Nullable String left, @Nullable String right) {
+    if (left == null || right == null) return false;
+    return left.trim().toLowerCase(Locale.US).equals(right.trim().toLowerCase(Locale.US));
+  }
+
+  private static HlsPlaylist repair(HlsPlaylist playlist) {
+    if (!(playlist instanceof HlsMultivariantPlaylist)) return playlist;
+    HlsMultivariantPlaylist master = (HlsMultivariantPlaylist) playlist;
+
+    Set<String> groups = new LinkedHashSet<>();
+    for (HlsMultivariantPlaylist.Rendition rendition : master.audios) {
+      // A null URI is muxed audio and cannot repair an external linkage.
+      if (rendition.url != null && !rendition.groupId.isEmpty()) groups.add(rendition.groupId);
+    }
+    if (groups.size() != 1) return master;
+
+    String declaredAudioGroup = groups.iterator().next();
+    boolean changed = false;
+    List<HlsMultivariantPlaylist.Variant> variants = new ArrayList<>(master.variants.size());
+    for (HlsMultivariantPlaylist.Variant variant : master.variants) {
+      boolean mismatched =
+          variant.audioGroupId != null && !groupIdsMatch(declaredAudioGroup, variant.audioGroupId);
+      // Only attach a missing AUDIO= when the master has no muxed audio and
+      // exactly one external audio group — otherwise the stream may already be
+      // muxed and guessing would attach the wrong language.
+      boolean missingAttach =
+          variant.audioGroupId == null && master.muxedAudioFormat == null;
+      if (mismatched || missingAttach) {
+        changed = true;
+        variants.add(
+            new HlsMultivariantPlaylist.Variant(
+                variant.url,
+                variant.format,
+                variant.videoGroupId,
+                declaredAudioGroup,
+                variant.subtitleGroupId,
+                variant.captionGroupId));
+      } else {
+        variants.add(variant);
+      }
+    }
+    if (!changed) return master;
+
+    return new HlsMultivariantPlaylist(
+        master.baseUri,
+        master.tags,
+        variants,
+        master.videos,
+        master.audios,
+        master.subtitles,
+        master.closedCaptions,
+        master.muxedAudioFormat,
+        master.muxedCaptionFormats,
+        master.hasIndependentSegments,
+        master.variableDefinitions,
+        master.sessionKeyDrmInitData);
+  }
+}
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt
index 1c7a55c..5410ecc 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt
@@ -53,6 +53,11 @@ import java.lang.ref.WeakReference
 class VideoPlayer(val context: Context, appContext: AppContext, source: VideoSource?) : AutoCloseable, SharedObject(appContext), IntervalUpdateEmitter {
   // This improves the performance of playing DRM-protected content
   private var renderersFactory = DefaultRenderersFactory(context)
+    // Use Media3 extension renderers (FFmpeg audio) only when the platform
+    // decoder cannot handle a format. ON keeps MediaCodec for AAC/etc. while
+    // still unlocking AC-3/E-AC-3/DTS through the app-bundled FFmpeg module.
+    // With no extension on the classpath this is a safe no-op.
+    .setExtensionRendererMode(DefaultRenderersFactory.EXTENSION_RENDERER_MODE_ON)
     .forceEnableMediaCodecAsynchronousQueueing()
     .setEnableDecoderFallback(true)
   private var listeners: MutableList<WeakReference<VideoPlayerListener>> = mutableListOf()
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt
index 0cdcc0b..ed948c3 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt
@@ -1,5 +1,6 @@
 package expo.modules.video.player
 
+import android.util.Log
 import androidx.annotation.OptIn
 import androidx.media3.common.C
 import androidx.media3.common.Format
@@ -20,12 +21,14 @@ class VideoPlayerAudioTracks(owner: VideoPlayer) : VideoPlayerListener {
       return owner.get()
     }
   private val formatsToGroups = mutableMapOf<Format, Pair<TrackGroup, Int>>()
+  private val formatIds = mutableMapOf<Format, String>()
   private var currentAudioTrackFormat: Format? = null
   private var currentOverride: TrackSelectionOverride? = null
 
   var currentAudioTrack: AudioTrack?
     get() {
-      return AudioTrack.fromFormat(currentAudioTrackFormat)
+      val format = currentAudioTrackFormat
+      return AudioTrack.fromFormat(format, true, format?.let { formatIds[it] })
     }
     set(value) {
       applyAudioTrack(value)
@@ -48,24 +51,34 @@ class VideoPlayerAudioTracks(owner: VideoPlayer) : VideoPlayerListener {
   // VideoPlayerListener
   override fun onTrackSelectionParametersChanged(player: VideoPlayer, trackSelectionParameters: TrackSelectionParameters) {
     currentAudioTrackFormat = findSelectedAudioFormat()
+    logSelectedTrack("parameters-changed")
     super.onTrackSelectionParametersChanged(player, trackSelectionParameters)
   }
 
   override fun onTracksChanged(player: VideoPlayer, tracks: Tracks) {
     formatsToGroups.clear()
+    formatIds.clear()
     availableAudioTracks.clear()
+    var groupIndex = 0
     for (group in tracks.groups) {
       for (i in 0..<group.length) {
         val format: Format = group.getTrackFormat(i)
 
         if (MimeTypes.isAudio(format.sampleMimeType)) {
           formatsToGroups[format] = group.mediaTrackGroup to i
-          val track = AudioTrack.fromFormat(format) ?: continue
+          // IPTV manifests often omit FORMAT ids and refine sample rate later.
+          // Include group/track indexes so JS selection stays stable across updates.
+          val identity =
+            "g$groupIndex:t$i:${format.sampleMimeType ?: "audio"}:${format.language ?: "und"}:${format.bitrate}"
+          formatIds[format] = format.id ?: identity
+          val track = AudioTrack.fromFormat(format, group.isTrackSupported(i), identity) ?: continue
           availableAudioTracks.add(track)
         }
       }
+      groupIndex += 1
     }
     currentAudioTrackFormat = findSelectedAudioFormat()
+    logSelectedTrack("tracks-changed")
     super.onTracksChanged(player, tracks)
   }
 
@@ -82,22 +95,26 @@ class VideoPlayerAudioTracks(owner: VideoPlayer) : VideoPlayerListener {
       currentOverride = null
       return
     }
-    val format = formatsToGroups.keys.firstOrNull {
-      it.id == audioTrack.id
+    val format = formatsToGroups.keys.firstOrNull { candidate ->
+      val candidateId = formatIds[candidate] ?: AudioTrack.fromFormat(candidate)?.id
+      candidateId == audioTrack.id
     }
     format?.let {
-      formatsToGroups[it]?.let { subtitlePair ->
-        val trackSelectionOverride = TrackSelectionOverride(subtitlePair.first, subtitlePair.second)
+      formatsToGroups[it]?.let { pair ->
+        val trackSelectionOverride = TrackSelectionOverride(pair.first, pair.second)
         newParameters = newParameters.buildUpon().addOverride(trackSelectionOverride).build()
         player.trackSelectionParameters = newParameters
         setAudioTracksEnabled(true)
         currentOverride = trackSelectionOverride
+        currentAudioTrackFormat = it
+        logSelectedTrack("apply")
       }
     }
   }
 
   private fun findSelectedAudioFormat(): Format? {
-    val trackSelectionParameters = videoPlayer?.player?.trackSelectionParameters
+    val player = videoPlayer?.player
+    val trackSelectionParameters = player?.trackSelectionParameters
     val preferredAudioLanguages = trackSelectionParameters?.preferredAudioLanguages
     val overriddenFormat: Format? = trackSelectionParameters?.overrides?.let {
       for ((group, trackSelectionOverride) in it) {
@@ -111,6 +128,22 @@ class VideoPlayerAudioTracks(owner: VideoPlayer) : VideoPlayerListener {
       return@let null
     }
 
+    // Prefer the track Media3 is actually rendering over a language preference guess.
+    val selectedFormat: Format? = player?.currentTracks?.groups?.firstNotNullOfOrNull { group ->
+      if (!group.isSelected) {
+        null
+      } else {
+        (0 until group.length).firstNotNullOfOrNull { index ->
+          if (!group.isTrackSelected(index)) {
+            null
+          } else {
+            val format = group.getTrackFormat(index)
+            if (MimeTypes.isAudio(format.sampleMimeType)) format else null
+          }
+        }
+      }
+    }
+
     val preferredFormat: Format? = preferredAudioLanguages?.let { preferredAudioLanguages ->
       for (preferredLanguage in preferredAudioLanguages) {
         return@let formatsToGroups.keys.firstOrNull {
@@ -120,6 +153,24 @@ class VideoPlayerAudioTracks(owner: VideoPlayer) : VideoPlayerListener {
       return@let null
     }
 
-    return overriddenFormat ?: preferredFormat
+    return overriddenFormat ?: selectedFormat ?: preferredFormat
+  }
+
+  private fun logSelectedTrack(reason: String) {
+    val format = currentAudioTrackFormat
+    if (format == null) {
+      Log.i(TAG, "audio[$reason] none tracks=${availableAudioTracks.size}")
+      return
+    }
+    Log.i(
+      TAG,
+      "audio[$reason] id=${formatIds[format] ?: format.id} mime=${format.sampleMimeType} " +
+        "lang=${format.language} ch=${format.channelCount} rate=${format.sampleRate} " +
+        "bitrate=${format.bitrate} tracks=${availableAudioTracks.size}"
+    )
+  }
+
+  companion object {
+    private const val TAG = "CharmIPTVAudio"
   }
 }
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerLoadControl.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerLoadControl.kt
index 4ffe72b..3590912 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerLoadControl.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerLoadControl.kt
@@ -50,7 +50,11 @@ class VideoPlayerLoadControl : DefaultLoadControl() {
       (bufferOptions.minBufferForPlayback * 1000).toLong()
     }
     bufferForPlaybackMs = safeBufferForPlayback
-    bufferForPlaybackAfterRebufferMs = safeBufferForPlayback
+    // Live IPTV should start quickly, but after a network dip require at least
+    // one second before resuming so we do not bounce straight back into BUFFERING.
+    // This mirrors the TiviMate-style 500ms start / 1000ms rebuffer split while
+    // preserving larger user-selected startup thresholds.
+    bufferForPlaybackAfterRebufferMs = maxOf(1_000L, safeBufferForPlayback)
 
     updateAllocator()
   }
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt
index 20dcbb7..3a0fbe0 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt
@@ -32,19 +32,41 @@ class SubtitleTrack(
 class AudioTrack(
   @Field val id: String,
   @Field val language: String?,
-  @Field val label: String?
+  @Field val label: String?,
+  /** Media3 sample MIME type, e.g. audio/mp4a-latm or audio/ac3. */
+  @Field val mimeType: String? = null,
+  /** Whether the current renderer reports this track as supported. */
+  @Field val isSupported: Boolean = true
 ) : Record, Serializable {
   companion object {
-    fun fromFormat(format: Format?): AudioTrack? {
+    fun fromFormat(
+      format: Format?,
+      isSupported: Boolean = true,
+      identity: String? = null,
+    ): AudioTrack? {
       format ?: return null
-      val id = format.id ?: return null
+      // IPTV HLS manifests often omit a stable FORMAT id. Keep the track
+      // selectable by deriving a deterministic identity instead of dropping it
+      // from availableAudioTracks altogether. Prefer an explicit identity that
+      // includes group/track indexes so later format refinement cannot collide.
+      val id = format.id
+        ?: identity
+        ?: listOf(
+          format.sampleMimeType ?: "audio",
+          format.language ?: "und",
+          format.channelCount.toString(),
+          format.sampleRate.toString(),
+          format.bitrate.toString()
+        ).joinToString(":")
       val language = format.language
       val label = language?.let { Locale(it).displayLanguage } ?: "Unknown"
 
       return AudioTrack(
         id = id,
         language = language,
-        label = label
+        label = label,
+        mimeType = format.sampleMimeType,
+        isSupported = isSupported
       )
     }
   }
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt
index 7e213f7..efcca31 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt
@@ -10,9 +10,30 @@ import androidx.media3.datasource.DefaultDataSource
 import androidx.media3.datasource.cache.CacheDataSource
 import androidx.media3.datasource.okhttp.OkHttpDataSource
 import androidx.media3.exoplayer.source.DefaultMediaSourceFactory
+import androidx.media3.exoplayer.hls.HlsMediaSource
 import androidx.media3.exoplayer.source.MediaSource
 import expo.modules.video.records.VideoSource
+import expo.modules.video.enums.ContentType
+import expo.modules.video.HlsAudioRecoveryPlaylistParserFactory
+import okhttp3.ConnectionPool
+import okhttp3.Dispatcher
 import okhttp3.OkHttpClient
+import java.util.concurrent.TimeUnit
+
+private object CharmPlayerHttpPool {
+  private val dispatcher = Dispatcher().apply {
+    maxRequests = 32
+    maxRequestsPerHost = 8
+  }
+  val client: OkHttpClient = OkHttpClient.Builder()
+    .dispatcher(dispatcher)
+    .connectionPool(ConnectionPool(15, 5, TimeUnit.MINUTES))
+    .connectTimeout(5, TimeUnit.SECONDS)
+    .readTimeout(5, TimeUnit.SECONDS)
+    .writeTimeout(5, TimeUnit.SECONDS)
+    .retryOnConnectionFailure(true)
+    .build()
+}
 
 @OptIn(UnstableApi::class)
 fun buildBaseDataSourceFactory(context: Context, videoSource: VideoSource): DataSource.Factory {
@@ -25,7 +46,7 @@ fun buildBaseDataSourceFactory(context: Context, videoSource: VideoSource): Data
 
 @OptIn(UnstableApi::class)
 fun buildOkHttpDataSourceFactory(context: Context, videoSource: VideoSource): OkHttpDataSource.Factory {
-  val client = OkHttpClient.Builder().build()
+  val client = CharmPlayerHttpPool.client
 
   // If the application name has ANY non-ASCII characters, we need to strip them out. This is because using non-ASCII characters
   // in the User-Agent header can cause issues with getting the media to play.
@@ -63,8 +84,18 @@ fun buildExpoVideoMediaSource(context: Context, videoSource: VideoSource): Media
   } else {
     buildBaseDataSourceFactory(context, videoSource)
   }
-  val mediaSourceFactory = buildMediaSourceFactory(context, dataSourceFactory)
   val mediaItem = videoSource.toMediaItem(context)
+  // IPTV master manifests frequently omit or misstate AUDIO/CODECS metadata.
+  // Chunkless preparation trusts that manifest and can leave a valid muxed
+  // audio PID undiscovered. For explicitly HLS media, inspect media chunks
+  // before publishing tracks; other source types retain Expo's default path.
+  if (videoSource.contentType == ContentType.HLS) {
+    return HlsMediaSource.Factory(dataSourceFactory)
+      .setAllowChunklessPreparation(false)
+      .setPlaylistParserFactory(HlsAudioRecoveryPlaylistParserFactory())
+      .createMediaSource(mediaItem)
+  }
+  val mediaSourceFactory = buildMediaSourceFactory(context, dataSourceFactory)
   return mediaSourceFactory.createMediaSource(mediaItem)
 }
 
diff --git a/node_modules/expo-video/build/VideoPlayer.types.d.ts b/node_modules/expo-video/build/VideoPlayer.types.d.ts
index 204f98d..f10de9a 100644
--- a/node_modules/expo-video/build/VideoPlayer.types.d.ts
+++ b/node_modules/expo-video/build/VideoPlayer.types.d.ts
@@ -551,5 +551,18 @@ export type AudioTrack = {
      * Label of the audio track in the language of the device.
      */
     label: string;
+    /**
+     * Media3 sample MIME type when the Android source exposes it, for example
+     * `audio/mp4a-latm`, `audio/ac3`, or `audio/eac3`.
+     *
+     * @platform android
+     */
+    mimeType?: string | null;
+    /**
+     * Whether the active Media3 renderer can decode this track.
+     *
+     * @platform android
+     */
+    isSupported?: boolean;
 };
 //# sourceMappingURL=VideoPlayer.types.d.ts.map
\ No newline at end of file
