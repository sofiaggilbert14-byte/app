# Optional playlist / EPG for direct-fetch builds (never commit real URLs).
# EXPO_PUBLIC_M3U_URL=https://provider.example/playlist.m3u
# EXPO_PUBLIC_EPG_URL=https://provider.example/guide.xml.gz

# Deployed Cloudflare Worker URL, without a trailing slash.
EXPO_PUBLIC_CHARM_API_URL=https://charm-iptv-api.example.workers.dev

# Optional web-preview proxy host (legacy backend). Native APK never uses this.
# EXPO_PUBLIC_BACKEND_URL=https://preview.example.com
