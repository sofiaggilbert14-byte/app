# Known Issues

- Native VLC playback cannot be validated in Expo Go or the web preview.
- The Cloudflare Worker requires a real KV namespace ID and deployment URL.
- Android TV and Fire TV behavior still requires physical-device verification.
- The native Android project is generated and committed, but its first Gradle build still needs network access to download the wrapper and dependencies.
